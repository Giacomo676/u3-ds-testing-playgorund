import {
  Meta,
  Canvas,
} from '@storybook/addon-docs/blocks'

import * as ButtonStories from './UltrusButton.stories'

<Meta
  of={ButtonStories}
  name="Change log"
/>

# Button — Change log

A record of design and implementation changes made to the ULTRUS Button component.

This page is intended to help designers and engineers understand what has changed from the existing MUI implementation and what is currently available in the ULTRUS 3.0 playground.

---

## 02/09/2026

### Primary Contained colour updated

The primary Contained Button colours have been updated.

| State | Primary Red |
| --- | --- |
| Enabled | 950 — `#1A000B` |
| Hover | 800 — `#5B0428` |
| Pressed | 950 — `#1A000B` |

### Contained

<Canvas of={ButtonStories.Contained} />

---

## Loading state updated

Loading Buttons now use a deliberately subdued visual treatment to make it clearer that the action is temporarily inactive.

The Button remains functionally disabled while loading.

### Without an icon

When no start or end icon is present:

- the label is visually hidden
- the original Button width is retained
- the spinner is centred

### With a start icon

The spinner replaces the start icon while the label remains visible.

### With an end icon

The spinner replaces the end icon while the label remains visible.

### Loading colours

#### Contained

Contained Loading uses:

- Primary Red 950
- White 40% overlay

#### Outlined

Outlined Loading uses:

- Secondary enabled surface
- Black 4% background treatment
- White 70% treatment over the foreground and stroke

#### Text

Text Loading uses:

- White 60% treatment over the text and icon colour

#### Destructive

Destructive Loading uses:

- Secondary enabled surface
- Black 4% background treatment
- White 70% treatment over the destructive foreground and stroke

---

## Button foundations updated

The ULTRUS Button has been moved away from the default MUI visual treatment while retaining MUI as the underlying component and API.

### Shape

The default MUI Button corner radius has been replaced with a fully rounded pill treatment.

### Heights

| Size | ULTRUS |
| --- | ---: |
| Large | 44px |
| Medium | 40px |
| Small | 32px |

### Horizontal padding

Large, Medium and Small Buttons use **16px horizontal padding**.

### Vertical padding

Large, Medium and Small Buttons use **8px vertical padding**.

### Icon spacing

The gap between the Button label and an optional start or end icon is:

**4px**

### Typography

| Size | Font size | Line height |
| --- | ---: | ---: |
| Large | 16px | 28px |
| Medium | 14px | 24px |
| Small | 12px | 24px |

---

## Variants

Four ULTRUS Button treatments are currently available:

- **Contained**
- **Outlined**
- **Text**
- **Destructive**

### Contained

<Canvas of={ButtonStories.Contained} />

### Outlined

<Canvas of={ButtonStories.Outlined} />

### Text

<Canvas of={ButtonStories.Text} />

### Destructive

<Canvas of={ButtonStories.Destructive} />

---

## Interaction changes

The MUI ripple interaction has been removed globally.

### Hover

Buttons use **Elevation 7** on hover.

### Focus

Keyboard focus uses the ULTRUS dashed focus outline.

Elevation is removed while focused.

### Pressed

Pressed Buttons do not use elevation.

---

## Disabled

Disabled Buttons use the shared ULTRUS disabled semantic colours.

<Canvas of={ButtonStories.Disabled} />

---

## Implementation principle

ULTRUS continues to use MUI as the underlying Button component.

**MUI owns the component API and behaviour. ULTRUS owns the visual language, semantic tokens, approved variants and design-system defaults.**